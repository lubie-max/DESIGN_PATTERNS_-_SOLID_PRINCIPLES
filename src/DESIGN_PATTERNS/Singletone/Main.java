package DESIGN_PATTERNS.Singletone;

public class Main {
    // The only one object can be created for the class
    // If we want to have only single authorization for the instance
    // like said .. API KEY for open APIs
    // database connection creds / strings etc.

}
